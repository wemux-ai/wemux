import assert from 'node:assert/strict'
import test from 'node:test'

// Tests for preserveLoadedMainChatSessions were removed as part of P1:
// main chat messages are no longer broadcast via AppState, so the
// merge/preserve logic is no longer needed.
